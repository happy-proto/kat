events {}

http {
  map $http_x_theme $selected_theme {
    default Dracula;
    dark Night;
  }

  server {
    listen 8080;
    location /preview {
      root /srv/kat;
      try_files $uri /index.html;
    }
  }
}
